# BetFlow 작업 지침

이 파일은 저장소 전체에 적용한다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 파일의
지침을 함께 적용하고, 충돌하는 경우 더 가까운 위치의 지침을 우선한다.

## 프로젝트 목적

BetFlow는 과거 스포츠 경기의 승·무·패 배당 변화 시퀀스를 수집·검증하고, 그 흐름을 경기 결과
예측에 활용하기 위한 프로젝트다. 초기 배당과 최종 배당만 비교하지 않고 공개된 중간 변경 순서를
보존하는 것이 핵심이다.

## 현재 우선순위와 범위

- 와이즈토토에서 확인한 2010~2026년 과거 배당 시퀀스의 전체 수집 파이프라인 구현을 우선한다.
- 수집기는 재시도, 요청 속도 제한, 체크포인트와 재개, 오류 격리, 중복·누락 검증을 지원해야 한다.
- 전체 데이터의 품질과 학습 가능 규모가 확정되기 전에는 예측 모델을 본격적으로 구현하지 않는다.
- 데이터 수집 단계와 직접 관련 없는 프론트엔드 확장 및 최종 데이터베이스 설계를 진행하지 않는다.
- 사용자가 요청한 작업 범위를 넘어선 리팩터링이나 기술 도입을 임의로 포함하지 않는다.

## 주요 경로

- `apps/api/app`: FastAPI 애플리케이션과 도메인 코드
- `apps/api/scripts`: 배당 이력 수집·검증용 실행 스크립트
- `apps/api/tests`: API와 수집·파싱 로직 테스트
- `apps/api/migrations`: Alembic 데이터베이스 마이그레이션
- `apps/web`: Next.js 웹 애플리케이션
- `docs`: PoC 결과, 데이터 제약, 검증 결론을 기록한 문서
- `artifacts`: 선별해 추적하는 검증 결과와 로컬 생성 산출물

## 개발 환경

- Python 버전은 3.12를 사용한다.
- Python 의존성과 잠금 파일은 `uv`로 관리한다.
- 프로젝트 루트의 `venv`를 활성화할 수 있으며, 활성화 후에는 `uv sync --project apps/api --active`로
  API 의존성을 동기화한다.
- Web 패키지는 pnpm workspace로 관리한다.
- 비밀정보와 실제 운영 환경 값은 커밋하지 않는다. 환경변수 예시는 `.env.example`에만 기록한다.
- 정확한 설치·실행 명령은 `README.md`를 기준으로 한다.

## 데이터 처리 원칙

- 수집한 원문과 원문에서 확인 가능한 변경 순서를 보존한다.
- 정확한 변경 시각이 없으면 값을 추정하지 말고 시각 부재를 명시한다.
- 승·무·패 변경 횟수 불일치나 최종 배당 불일치를 임의 보간·덮어쓰기하지 않는다.
- 정상 데이터와 파싱·검증 실패 데이터를 분리하고, 실패 사유와 재현 가능한 원문을 보존한다.
- 경기 식별자, 회차, 결과, 시퀀스 순서의 중복·누락·정합성을 검증한다.
- 공개 사이트가 전체 변경 이력을 보장한다고 가정하지 않는다.
- 외부 HTML·AJAX 구조 변경을 감지할 수 있도록 파서 회귀 테스트와 실패 기록을 유지한다.
- 체크포인트와 임시 PoC 결과 같은 재생 가능한 산출물은 `.gitignore` 정책을 따른다.
- 새로운 대용량 데이터 파일은 필요성과 추적 정책을 확인하기 전까지 Git에 추가하지 않는다.

## 변경 안전성

- 기존의 미커밋 변경과 미추적 파일은 사용자 소유로 간주하고 임의로 삭제·복원·덮어쓰지 않는다.
- 요청받지 않은 파일 이동, 데이터 삭제, 의존성 추가, 설정 변경을 하지 않는다.
- 실패하는 테스트를 삭제하거나 검증을 우회하는 방식으로 작업을 완료하지 않는다.
- 데이터 삭제나 대규모 재생성이 필요하면 대상과 복구 가능성을 먼저 확인하고 사용자 승인을 받는다.

## 작업 시작 절차

1. `AGENTS.md`와 `README.md`를 먼저 확인한다.
2. `git status`, 현재 브랜치, 최근 관련 커밋을 확인한다.
3. 새 세션이나 큰 작업에서는 주요 엔트리포인트와 관련 문서를 통해 구조를 먼저 파악한다.
4. 저장소 전체를 무작정 읽지 말고 요청과 관련된 코드, 테스트, 설정만 추가로 조사한다.
5. 구현 요청이 아니라 상태 점검·설명 요청이면 파일을 수정하지 않는다.
6. 구현 전에는 포함 범위와 제외 범위, 예상 변경 파일, 구현 순서, 테스트 방법을 명확히 한다.
7. 작업 트리가 깨끗하지 않으면 기존 변경의 목적을 확인하고 새 작업과 섞지 않는다.

작은 문서 수정이나 같은 세션의 후속 작업에는 필요한 확인만 수행한다. 데이터베이스, 인증,
아키텍처, 배포·CI처럼 영향 범위가 큰 변경에는 전체 검증과 관련 문서 확인을 강화한다.

## 구현 원칙

- 기존 아키텍처와 코드 스타일을 따르고 변경 범위를 최소화한다.
- 새 기능과 버그 수정에는 정상 경로뿐 아니라 실패·경계 사례 테스트를 함께 고려한다.
- 네트워크 수집 코드는 적절한 요청 간격, 제한된 재시도, 명확한 타임아웃을 사용한다.
- 장시간 수집은 중단 후 안전하게 재개할 수 있어야 하며, 완료된 작업을 중복 처리하지 않아야 한다.
- 로그에는 회차·경기 등 재현에 필요한 문맥을 남기되 비밀정보와 과도한 원문을 노출하지 않는다.
- 새로운 라이브러리, 서비스, 메시지 브로커는 현재 요구사항으로 필요성이 확인된 경우에만 도입한다.

## 검증 명령

API 관련 변경은 프로젝트 루트의 `venv`를 활성화하고 `apps/api`에서 다음 명령을 기본으로 실행한다.

```powershell
pytest
ruff check app migrations scripts tests
```

Web 관련 변경은 프로젝트 루트에서 다음 명령을 변경 범위에 맞게 실행한다.

```powershell
pnpm.cmd --filter @betflow/web lint
pnpm.cmd --filter @betflow/web typecheck
pnpm.cmd --filter @betflow/web build
```

- 작은 변경은 관련 테스트부터 실행하고, 영향 범위가 넓으면 전체 테스트를 실행한다.
- 문서만 변경했다면 코드 전체 테스트를 기계적으로 실행하지 않아도 되지만 명령, 경로, 링크는 검증한다.
- 검증이 실패하면 원인을 분석하고 실패 사실과 미실행 검증을 명확히 보고한다.
- 커밋 전 `git diff --check`, `git status`, 관련 diff를 확인한다.

## Git 및 커밋 규칙

- 새 작업은 깨끗하고 최신화된 `main`에서 목적별 브랜치를 만들어 진행한다.
- 브랜치 prefix는 `feat/`, `fix/`, `docs/`, `chore/`, `spike/`를 사용한다.
- 서로 독립적으로 설명 가능한 변경은 논리적인 커밋으로 나눈다. 각 커밋은 가능한 한 검증 가능한
  상태를 유지한다.
- `git add .`을 기본값으로 사용하지 않는다. 작업 목적에 해당하는 파일만 선택적으로 스테이징한다.
- 커밋 직전 `git diff --cached`와 필요하면 `git diff --cached --stat`으로 실제 포함 내용을 확인한다.
- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `chore:` 등 소문자 Conventional
  Commits prefix와 한글 설명을 사용한다. 기술 검증에는 프로젝트 규칙으로 `spike:`를 사용할 수 있다.
- 사용자가 명시적으로 요청하거나 변경 내용을 확인하고 승인한 경우에만 커밋한다.
- push, PR 생성, merge는 사용자 요청 없이 수행하지 않는다.

## 문서 관리

- 프로젝트 목적, 설치·실행·사용 방법, 환경 설정이 달라지면 `README.md` 갱신 여부를 확인한다.
- 지속적인 작업 규칙, 아키텍처 결정, 검증 명령, 중요한 제약이 달라지면 `AGENTS.md`를 갱신한다.
- 일별 진행 상황, 특정 커밋·PR 내역, 일회성 조사 메모를 `AGENTS.md`에 기록하지 않는다.
- 데이터 조사 결과와 판단 근거는 `docs`에, 선별한 재현 자료는 `artifacts`에 기록한다.
- 문서 변경이 필요하지 않다면 작업 내역을 남기기 위해 억지로 수정하지 않는다.

## 완료 기준

- 요청된 범위의 구현과 관련 테스트 작성 완료
- 관련 테스트, lint, type check, build 중 필요한 검증 통과
- 의도하지 않은 변경, 디버깅 코드, 비밀정보, 불필요한 대용량 산출물 부재
- 예외 처리와 기존 기능 영향 검토 완료
- `README.md`와 `AGENTS.md` 갱신 필요 여부 확인
- 변경 파일, 검증 결과, 남은 위험이나 후속 작업 보고
