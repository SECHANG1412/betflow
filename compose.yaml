services:
  postgres:
    image: postgres:17-alpine
    container_name: betflow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-betflow}
      POSTGRES_USER: ${POSTGRES_USER:-betflow}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-betflow_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-betflow} -d ${POSTGRES_DB:-betflow}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

